Evaluate EinScan-pro:
    Setup a _VNC_ server on erik's machine, to test scanning, as RDP doesn't seem to support CUDA, which this seems to use.
    Scan some things! (Possibly plant-like things)

CMake ALL the rest of the ER_port & port it.
    #Ported(?)
	-FragmentOptimizer
	-GraphOptimizer
    #Not yet ported.
	-Integrate
	-GlobalRegistration


PCL <-> Compile, play with /test

Papers <-> Read them, make some nice summaries.

OpenNI2 <-> getting pictures & depth pictures, turning on IR light for scanner?
